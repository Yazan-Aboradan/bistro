import 'package:flutter/material.dart';

import 'widgets/views/auth_login_content_view.dart';

class AuthLoginContent extends StatelessWidget {
  const AuthLoginContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const AuthLoginContentView();
  }
}
